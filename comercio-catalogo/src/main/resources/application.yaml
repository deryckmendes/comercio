spring:
  application:
    name: comercio-catalogo
  mongodb:
    uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/catalogo}
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

api:
  security:
    token:
      secret: ${JWT_SECRET:minha-chave-de-seguranca}

server:
  port: 8081
