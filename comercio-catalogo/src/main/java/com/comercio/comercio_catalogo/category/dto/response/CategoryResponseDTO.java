package com.comercio.comercio_catalogo.category.dto.response;

// sem anotações, apenas o objeto plano para transferência de dados (DTO)
public record CategoryResponseDTO(
                String id,
                String name) {
}
